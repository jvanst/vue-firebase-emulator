{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start Firebase Emulator",
      "type": "shell",
      "command": "firebase emulators:start --project demo-app --import ./data --export-on-exit",
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}/firebase-emulator"
      }
    }
  ]
}
